definitions:
  config:
    /hippo:configuration/hippo:groups/webmaster:
      hipposys:description: BloomReach's default webmaster group. The property hipposys:system
        has been set to true to prevent this group from appearing in the list of groups
      hipposys:system: true
