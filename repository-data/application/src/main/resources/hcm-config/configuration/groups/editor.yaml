definitions:
  config:
    /hippo:configuration/hippo:groups/editor:
      hipposys:description: BloomReach's default editor group. The property hipposys:system
        has been set to true to prevent this group from appearing in the list of groups
      hipposys:system: true
